面向对象
继承：
virtual 修辞符 将允许重写的每个成员标记为Virtual
方法声明前带有virtual关键字，那么这个方法就是虚方法，虚方法的实现可以由派生类所取代，这种取代是通过方法的重写实现的
特点：

1.虚方法不允许有static修饰符

2.虚方法不允许有abstract修饰符

3.虚方法不允许有override修饰符

4.虚方法不能是私有的，不能使用private修饰符

sealed 修饰符，禁止子类继续重写该成员

abstract 修饰符定义抽象类
抽象成员必须被重写，会自动成为虚成员，但是不能使用Virtual关键字显声明


Override关键字主要是提供派生类对基类方法的新实现，重写的基类方法必须和Override的方法具有相同的签名

implicit 关键字用于声明隐式的用户定义类型转换运算符。如果可以确保转换过程不会造成数据丢失，则可使用该关键字在用户定义类型和其他类型之间进行隐式转换

接口：
接口的名称要使用Pascal 大小写风格，并以"I"作为前缀
接口使用Interface关键字声明，默认是public
如果一个接口继承其他接口，那么实现类或结构就需要实现所有接口的成员。
接口方法不能用public abstract等修饰。接口内不能有字段变量，构造函数。
接口内可以定义属性（有get和set的方法）。如string color { get ; set ; }
实现接口时，必须和接口的格式一致。
必须实现接口的所有方法。

值类型：
不要创建消耗内存大于十六字节的值类型
引用类型是





